<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <ImageView
        android:id="@+id/destinationImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:src="@android:drawable/ic_menu_mylocation" />

    <EditText
        android:id="@+id/destinationBox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/destinationImage"
        android:layout_marginTop="22dp"
        android:layout_toLeftOf="@+id/goButton"
        android:ems="10" />

    <Button
        android:id="@+id/goButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@+id/destinationImage"
        android:layout_marginRight="14dp"
        android:text="Go"
        android:onClick="startNavigating" />

    <EditText
        android:id="@+id/routeTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/destinationBox"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/destinationBox"
        android:ems="10"
        android:inputType="textMultiLine" />

</RelativeLayout>
